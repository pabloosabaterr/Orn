struct Person {
    age: int
    score: int
};

let player: Person;
player.age = 25;
player.score = 100;

// Give bonus points
const bonus: int = 10;
player.score += bonus;

print(player.age);    // Should print 25
print("\n");
print(player.score);  // Should print 110