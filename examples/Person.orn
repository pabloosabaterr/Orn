import "../lib/stdio";

struct Person {
    age: int
    score: int
};

let player: Person;
player.age = 25;
player.score = 100;

// Give bonus points
const bonus: int = 10;
player.score += bonus;

print_int(player.age);    // Should print 25
print_string("\n");
print_int(player.score);  // Should print 110