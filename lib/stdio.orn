import "str/itoa";

export fn print_int(n: i64) -> void {
    let buf: char[32];
    let i: i64 = 0;
    while i < 32 {
        buf[i] = 0 as char;
        ++i;
    }
    let ptr: *char = &buf[0];
    let start: *char = itoa(n, ptr, 32);
    let end: *char = ptr + 31;
    let len: i64 = end - start;
    syscall(1, 1, start, len, 0, 0, 0);
}